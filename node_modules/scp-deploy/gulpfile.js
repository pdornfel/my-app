var gulp = require('gulp');
var babel = require('gulp-babel');
 
gulp.task('default', () => {
    return gulp.src('lib/scp-deploy.js')
        .pipe(babel({
            presets: ['es2015']
        }))
        .pipe(gulp.dest('dest/'));
});